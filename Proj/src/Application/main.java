package Application;

import Controller.DashboardForm;
import Controller.RegistrationForm;
import Controller.LoginForm;
import  Model.User;
public class main {
    public static void main(String[] args) {
        DashboardForm myForm = new DashboardForm();
        /*RegistrationForm myReg = new RegistrationForm(null);
        Model.User user = myReg.user;
        if (user != null) {
            System.out.println("Successful registration of: " + user.firstName + " " + user.lastName);
        }
        else {
            System.out.println("Registration canceled");
        }
        */
        /*
        Controller.LoginForm loginForm = new Controller.LoginForm(null);
        Model.User login = loginForm.user;
        if (login != null) {
            System.out.println("Successful Authentication of: " + login.firstName + login.lastName);
            System.out.println("          Footballer: " + user.footballer);
            System.out.println("          Phone: " + user.phone);

        }
        else {
            System.out.println("Authentication canceled");
        }
    }*/
    }


    }
